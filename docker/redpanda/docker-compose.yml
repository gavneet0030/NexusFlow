name: nexusflow-redpanda

networks:
  nexusflow-redpanda:
    driver: bridge

volumes:
  nexusflow-redpanda-data:

services:

  redpanda:
    image: docker.redpanda.com/redpandadata/redpanda:v26.2.3
    container_name: nexusflow-redpanda
    command:
      - redpanda
      - start
      - --kafka-addr
      - internal://0.0.0.0:9092,external://0.0.0.0:19092
      - --advertise-kafka-addr
      - internal://redpanda:9092,external://localhost:19092
      - --pandaproxy-addr
      - internal://0.0.0.0:8082,external://0.0.0.0:18082
      - --advertise-pandaproxy-addr
      - internal://redpanda:8082,external://localhost:18082
      - --schema-registry-addr
      - internal://0.0.0.0:8081,external://0.0.0.0:18081
      - --rpc-addr
      - redpanda:33145
      - --advertise-rpc-addr
      - redpanda:33145
      - --mode
      - dev-container
      - --smp
      - "1"
      - --default-log-level=info

    volumes:
      - nexusflow-redpanda-data:/var/lib/redpanda/data

    networks:
      - nexusflow-redpanda

    ports:
      - "19092:19092"
      - "18081:18081"
      - "18082:18082"
      - "19644:9644"

    healthcheck:
      test:
        [
          "CMD",
          "rpk",
          "cluster",
          "info",
          "--brokers",
          "localhost:9092"
        ]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  console:
    image: docker.redpanda.com/redpandadata/console:v3.12.0
    container_name: nexusflow-redpanda-console

    networks:
      - nexusflow-redpanda

    environment:
      CONFIG_FILEPATH: /tmp/config.yml

      CONSOLE_CONFIG_FILE: |
        kafka:
          brokers:
            - redpanda:9092

        schemaRegistry:
          enabled: true
          urls:
            - http://redpanda:8081

        redpanda:
          adminApi:
            enabled: true
            urls:
              - http://redpanda:9644

    ports:
      - "18080:8080"

    depends_on:
      redpanda:
        condition: service_healthy
