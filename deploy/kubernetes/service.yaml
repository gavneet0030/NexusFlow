apiVersion: v1
kind: Service
metadata:
  name: nexusflow
  namespace: nexusflow
  labels:
    app.kubernetes.io/name: nexusflow
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nexusflow
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
