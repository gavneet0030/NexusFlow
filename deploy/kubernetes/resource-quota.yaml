apiVersion: v1
kind: ResourceQuota
metadata:
  name: nexusflow-quota
  namespace: nexusflow
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 1Gi
    limits.cpu: "4"
    limits.memory: 2Gi
    pods: "10"
